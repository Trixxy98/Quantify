# Salin fail ni jadi ".env" (di root projek). JANGAN commit ".env" ke git.
# Guna value ni untuk docker-compose.yml (Postgres container).

POSTGRES_USER=quantify
POSTGRES_PASSWORD=quantify
POSTGRES_DB=quantify
POSTGRES_PORT=5434
