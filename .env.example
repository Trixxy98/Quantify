# Copy this file to ".env" (at the project root). Do not commit ".env".
# Used by docker-compose.yml (Postgres container).

POSTGRES_USER=quantify
POSTGRES_PASSWORD=quantify
POSTGRES_DB=quantify
POSTGRES_PORT=5434
